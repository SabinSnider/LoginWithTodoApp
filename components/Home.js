import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Home = props => {
  return (
    <View>
      <Text> HEllo.</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
